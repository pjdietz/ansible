- name: Install packages
  package:
    # TODO: extract cargo install and add cargo install tree-sitter-cli
    name:
      - cargo
      - fzf
      - ripgrep
      - tmux
      - tree
      - zoxide
    state: present
  tags:
    - install
    - productivity
